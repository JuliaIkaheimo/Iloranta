import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
      height: '88%',
      justifyContent: "center"
    },
    mapStyle: {
        width: '100%',
        height: '100%'
    },
    titleText: {
      fontSize: 18,
    }

 });