import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
      height: '100%',
      justifyContent: "center"
    },
    mapStyle: {
        width: '100%',
        height: '100%'
    },
    markerStyle: {
      borderRadius: 5,
      height: 50,
      width: 50,
    },

 });