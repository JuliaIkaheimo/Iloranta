import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
    container: {
      height: '87%',
      justifyContent: "center"
    },
    mapStyle: {
        width: '100%',
        height: '100%'
    }
  });